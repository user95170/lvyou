---
description: 
auto_execution_mode: 3
---

# 真实数据获取实施方案

> **创建时间**：2025-11-17  
> **目的**：为旅游推荐系统获取真实、合法、可用的数据

---

## ⚖️ 法律与合规说明

### 必须遵守的原则

1. **尊重 robots.txt 协议**：访问网站前检查 robots.txt
2. **遵守服务条款（ToS）**：不违反网站使用协议
3. **合理请求频率**：设置速率限制，避免影响服务器
4. **仅用于学术研究**：数据仅用于毕业设计，不商用
5. **尊重版权**：不公开分享原始数据

### 推荐的数据获取方式（按优先级）

| 方式 | 合法性 | 难度 | 数据质量 | 推荐度 |
|------|--------|------|----------|--------|
| 官方开放数据 | ✅ 完全合法 | 低 | 高 | ⭐⭐⭐⭐⭐ |
| 公开API（免费额度） | ✅ 合法 | 中 | 高 | ⭐⭐⭐⭐⭐ |
| 学术数据集 | ✅ 合法 | 低 | 中 | ⭐⭐⭐⭐ |
| 政府网站公开数据 | ✅ 合法 | 中 | 高 | ⭐⭐⭐⭐ |
| 合规爬虫（遵守robots.txt） | ⚠️ 需谨慎 | 高 | 高 | ⭐⭐⭐ |

---

## 📊 推荐的数据源

### 1. 高德地图开放平台（首选）

**优势**：

- 官方API，完全合法
- 免费配额充足（个人开发者每日30万次调用）
- 数据质量高、更新及时
- 支持POI搜索、详情、周边查询

**申请步骤**：

1. 注册高德开放平台账号：[高德开放平台](https://lbs.amap.com/)
2. 进入控制台 → 应用管理 → 创建应用
3. 添加Key（选择"Web服务"类型）
4. 获取API Key（类似：`a1b2c3d4e5f6...`）

**可获取的数据**：

- POI基础信息（名称、地址、坐标、电话）
- 景点类别、评分、营业时间
- 周边设施、交通信息
- 图片链接（部分）

**API文档**：

- POI搜索：[Web 服务 POI 搜索](https://lbs.amap.com/api/webservice/guide/api/search)
- POI详情：[Web 服务 POI 详情](https://lbs.amap.com/api/webservice/guide/api/detail)

**限制**：

- 每天30万次免费调用（超出需付费）
- 单次最多返回1000条结果
- 需要遵守服务条款

**实施优先级**：⭐⭐⭐⭐⭐（强烈推荐，立即可用）

---

### 2. 公开旅游数据集

**数据集来源**：

#### A. Kaggle 数据集

- **China Tourist Attractions Dataset**
  - 链接：搜索 "China tourism dataset" on Kaggle
  - 内容：中国主要城市景点数据
  - 格式：CSV/JSON
  - 免费下载

#### B. HyperAI 数据集

- **China City Attraction Details**
  - 链接：[hyper.ai/datasets/37330](https://hyper.ai/datasets/37330)
  - 内容：352个城市，每城市100个景点
  - 字段：名称、地址、介绍、开放时间、评分、推荐时长等
  - 格式：CSV

#### C. GitHub 开源数据

- 搜索关键词：`china tourism data`, `scenic spots dataset`
- 很多研究者会分享数据集

**实施优先级**：⭐⭐⭐⭐（备选方案，快速可用）

---

### 3. 政府公开数据

**数据来源**：

#### A. 文化和旅游部

- 官网：[mct.gov.cn](http://www.mct.gov.cn/)
- 数据：A级景区名单、旅游统计数据
- 格式：PDF/Excel（需要手动整理）

#### B. 内蒙古文化和旅游厅

- 官网：[wlt.nmg.gov.cn](http://wlt.nmg.gov.cn/)
- 数据：内蒙古景区信息、旅游线路
- 格式：网页/PDF

#### C. 国家统计局

- 官网：[stats.gov.cn](http://www.stats.gov.cn/)
- 数据：旅游人次、收入等宏观数据
- 用途：作为背景数据和权重调整依据

**实施优先级**：⭐⭐⭐（辅助数据，需要手动整理）

---

## 🚀 推荐实施方案（分阶段）

### 阶段1 高德地图POI数据（1-2天）

**目标**：获取内蒙古主要城市的景点、酒店、美食数据

**步骤**：

1. **申请高德API Key**
   - 注册账号，创建应用，获取Key
   - 配置到 `.env` 文件

2. **实现高德地图采集器**
   - 创建 `amap_collector.py`
   - 实现POI搜索接口调用
   - 支持分页、类型筛选

3. **采集数据**
   - 景点：关键词搜索（"景点"/"旅游景点"/"风景区"）
   - 酒店：类型码搜索（"100100"）
   - 美食：类型码搜索（"050000"）

4. **数据清洗与入库**
   - 去重
   - 地址解析
   - 写入数据库

**预期成果**：

- 景点：200-500条
- 酒店：100-200条
- 美食：100-200条

---

### 阶段2 公开数据集补充（1天）

**目标**：下载并导入公开数据集，补充数据量和多样性

**步骤**：

1. **下载数据集**
   - HyperAI：China City Attraction Details
   - Kaggle：搜索相关数据集

2. **数据映射**
   - 将数据集字段映射到内部表结构
   - 处理缺失字段

3. **合并去重**
   - 与高德数据合并
   - 基于名称+城市去重

---

### 阶段3 政府数据补充（可选，1-2天）

**目标**：获取官方权威数据，补充景区等级等信息

**步骤**：

1. **手动采集**
   - 访问文旅厅网站
   - 整理A级景区名单
   - 整理为CSV

2. **数据增强**
   - 为已有景点补充等级信息
   - 更新权威字段

---

## 💻 技术实施细节

### 高德地图API调用示例

```python
import requests
import time

class AmapCollector:
    def __init__(self, api_key):
        self.api_key = api_key
        self.base_url = "https://restapi.amap.com/v3/place"
    
    def search_poi(self, keywords, city="呼和浩特", poi_type=""):
        """搜索POI"""
        url = f"{self.base_url}/text"
        params = {
            'key': self.api_key,
            'keywords': keywords,
            'city': city,
            'types': poi_type,
            'offset': 20,  # 每页20条
            'page': 1,
            'extensions': 'all'  # 返回详细信息
        }
        
        response = requests.get(url, params=params)
        data = response.json()
        
        if data['status'] == '1':
            return data['pois']
        else:
            print(f"API调用失败: {data.get('info')}")
            return []
    
    def get_poi_detail(self, poi_id):
        """获取POI详情"""
        url = f"{self.base_url}/detail"
        params = {
            'key': self.api_key,
            'id': poi_id
        }
        
        response = requests.get(url, params=params)
        data = response.json()
        
        if data['status'] == '1':
            return data['pois'][0] if data['pois'] else None
        return None

# 使用示例
api_key = "your_amap_key_here"
collector = AmapCollector(api_key)

# 搜索呼和浩特的景点
pois = collector.search_poi(keywords="景点", city="呼和浩特")
for poi in pois:
    print(f"{poi['name']} - {poi['address']}")
    time.sleep(0.1)  # 速率限制
```

---

## 📝 数据字段映射

### 高德POI → 内部数据库

```python
# 景点映射
{
    'name': poi['name'],
    'city': poi['cityname'],
    'address': poi['address'],
    'longitude': poi['location'].split(',')[0],
    'latitude': poi['location'].split(',')[1],
    'type': poi['type'],  # 分类代码
    'phone': poi.get('tel', ''),
    'business_hours': poi.get('business_area', ''),
    'tags': ','.join(poi.get('tag', [])),
    'source': 'amap',
    'external_id': poi['id']
}
```

---

## ⚠️ 注意事项

### 1. API速率限制

```python
import time

def rate_limit(func):
    """装饰器：强制请求间隔"""
    def wrapper(*args, **kwargs):
        result = func(*args, **kwargs)
        time.sleep(0.1)  # 100ms间隔
        return result
    return wrapper
```

### 2. 错误处理

```python
import requests
from requests.adapters import HTTPAdapter
from requests.packages.urllib3.util.retry import Retry

def create_session():
    """创建带重试的Session"""
    session = requests.Session()
    retry = Retry(
        total=3,
        backoff_factor=1,
        status_forcelist=[500, 502, 503, 504]
    )
    adapter = HTTPAdapter(max_retries=retry)
    session.mount('http://', adapter)
    session.mount('https://', adapter)
    return session
```

### 3. 数据验证

```python
def validate_poi_data(poi):
    """验证POI数据完整性"""
    required_fields = ['name', 'location']
    for field in required_fields:
        if field not in poi or not poi[field]:
            return False
    return True
```

---

## 📊 预期成果

### 数据规模

- **景点**：500+ 条（覆盖内蒙古主要城市）
- **酒店**：200+ 条
- **美食**：200+ 条
- **总计**：900+ 条真实数据

### 数据质量

- ✅ 地址、坐标准确
- ✅ 联系方式有效
- ✅ 营业时间真实
- ✅ 分类标准化

### 时间成本

- 阶段1（高德API）：1-2天
- 阶段2（数据集）：1天
- 阶段3（政府数据）：1-2天（可选）
- **总计**：2-5天

---

## 🎯 立即可执行的最小方案

如果时间紧张，**最小可行方案**：

1. **申请高德API Key**（10分钟）
2. **运行高德采集脚本**（我会提供，30分钟编写+测试）
3. **采集1-2个城市数据**（1小时）
4. **导入数据库**（自动，10分钟）

**总耗时**：约2小时即可获得100+条真实数据

---

## 📚 相关资源

### API文档

- 高德地图开放平台：[https://lbs.amap.com/](https://lbs.amap.com/)
- 高德POI搜索API：[https://lbs.amap.com/api/webservice/guide/api/search](https://lbs.amap.com/api/webservice/guide/api/search)

### 数据集

- HyperAI：[https://hyper.ai/](https://hyper.ai/)
- Kaggle：[https://www.kaggle.com/datasets](https://www.kaggle.com/datasets)

### 法律参考

- 《数据安全法》
- 《个人信息保护法》
- 网络爬虫法律边界

---

> **建议**：优先使用高德地图API，合法合规且数据质量高。公开数据集作为补充。避免爬取需要登录或明确禁止的网站。
> **最后更新**：2025-11-17 22:15
